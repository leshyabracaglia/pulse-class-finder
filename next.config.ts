import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Allow the src/pages directory to coexist during migration
  // (Next.js will use src/app for routing)
};

export default nextConfig;
